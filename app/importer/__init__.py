"""Corpus importers: build the mock DB from a corpus.

- ``app.importer.erb`` — EnterpriseRAG-Bench (download + load + ACL, faithful/structured).
- ``app.importer.byo`` — Bring-Your-Own JSONL (validate + load).
"""
