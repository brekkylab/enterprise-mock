{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://brekkylab.dev/schemas/google_drive.schema.json",
  "title": "BYO corpus record — google_drive",
  "description": "A Google Drive file. Content is returned by files.export; subtype picks the Workspace type / export format.",
  "type": "object",
  "properties": {
    "source_type": {
      "const": "google_drive"
    },
    "content": {
      "type": "string",
      "minLength": 1,
      "description": "The retrieval payload served verbatim as the body."
    },
    "title": {
      "type": "string",
      "description": "Document title (email subject / file name / issue title / page title)."
    },
    "doc_id": {
      "type": "string",
      "description": "Stable id (default: dsid_<sha256(source_type+title+content)>)."
    },
    "folder": {
      "type": "string",
      "description": "The folder this document belongs to (default: the source_type)."
    },
    "group": {
      "type": [
        "string",
        "null"
      ],
      "description": "ACL group owning the folder (default: slug(container)); null means the container owns no group, so nothing is group-scoped to it."
    },
    "author_email": {
      "type": "string",
      "format": "email",
      "description": "Author / sender / owner."
    },
    "author_name": {
      "type": "string",
      "description": "The owner's display name as the corpus writes it, served as the document's owner (a name is not recoverable from an email address)."
    },
    "author_groups": {
      "type": "array",
      "items": {
        "type": "string"
      },
      "description": "Groups the author belongs to."
    },
    "visibility": {
      "enum": [
        "public",
        "group",
        "private"
      ],
      "description": "public = org-wide, group = the container's group, private = author only."
    },
    "readers": {
      "type": "array",
      "items": {
        "type": "string"
      },
      "description": "Explicit reader principals. `user:<email>` / `group:<id>` / `org:<name>` state the type; unprefixed, an address is a user and anything else a group. Overrides visibility — and an EMPTY array means nobody may read the document (admin-only), not everybody."
    },
    "meta": {
      "type": "object",
      "description": "Free-form per-source structured extras merged into the record."
    },
    "subtype": {
      "enum": [
        "csv",
        "doc",
        "document",
        "docx",
        "folder",
        "jpeg",
        "jpg",
        "json",
        "pdf",
        "png",
        "ppt",
        "pptx",
        "presentation",
        "spreadsheet",
        "txt",
        "xls",
        "xlsx",
        "zip"
      ],
      "description": "Drive file type (default: document). The four Workspace native types (`document`/`spreadsheet`/`presentation`/`folder`) get a `application/vnd.google-apps.*` mimeType and an /edit link; every other value is a binary file kind whose mimeType comes from `mime_type`, or from the value itself when it names a known extension."
    },
    "mime_type": {
      "type": "string",
      "description": "Override the emitted mimeType."
    },
    "parents": {
      "type": "array",
      "items": {
        "type": "string"
      },
      "description": "Parent folder ids."
    },
    "created": {
      "type": [
        "integer",
        "number",
        "string"
      ],
      "description": "createdTime (epoch seconds or ISO 8601). Default: synthesized from doc_id."
    },
    "updated": {
      "type": [
        "integer",
        "number",
        "string"
      ],
      "description": "modifiedTime (epoch seconds or ISO 8601). Default: created + 1h."
    },
    "trashed": {
      "type": "boolean",
      "description": "Whether the file is trashed (files.list excludes trashed by default)."
    },
    "collaborators": {
      "type": "array",
      "items": {
        "type": "string"
      },
      "description": "Collaborator principals (emails) — served as the file's non-owner permissions."
    }
  },
  "required": [
    "source_type",
    "content",
    "title"
  ],
  "additionalProperties": false
}
