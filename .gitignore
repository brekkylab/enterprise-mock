.venv/
__pycache__/
*.pyc
# All data/ contents are downloaded/generated by scripts (seed files, slice zips,
# mock.sqlite, tokens.yaml) — never committed.
/data/
# DB the bench example builds (generated; downloads a slice) — never committed.
/examples/import-enterpriserag-bench/data/
.pytest_cache/
*.egg-info/
# `python -m build` output (Task 6, Task 11). Not egg-info, so *.egg-info/ above missed it.
/dist/
/build/
.env

# Superpowers SDD scratch (progress ledgers, plans, specs) — not tracked.
.superpowers/
docs/superpowers/
# Agent-created git worktrees (a full checkout each) — never committed.
.claude/worktrees/

# Node deps + build output for the one TypeScript example (examples/using-official-sdk/linear/).
node_modules/
*.tsbuildinfo
/examples/using-official-sdk/linear/dist/

# Editor / OS cruft.
.DS_Store
.idea/
.vscode/
.ruff_cache/

/experiments/
